ne-strong) 0 4px, transparent 4px 8px); }
-.schedule-gap strong { font-weight: 650; }
-.schedule-conflict { min-height: 36px; padding: 8px 10px; border-radius: 10px; display: flex; align-items: center; gap: 8px; background: var(--danger-soft); color: #b1283a; font-size: 10.5px; }
-.schedule-conflict .icon { width: 16px; height: 16px; }
-.schedule-conflict strong { font-weight: 700; }
-.end-marker { margin-left: 72px; display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 10px; padding: 8px 2px 0; color: var(--text-soft); font-size: 10px; }
-.end-marker i { height: 1px; background: var(--line); }
-.end-marker strong { font-weight: 600; }
-
-.is-drag-source .activity-card { opacity: .2; box-shadow: none; }
-.drag-floating { position: fixed !important; z-index: 1000; pointer-events: none; transform: rotate(.35deg) scale(1.012); box-shadow: var(--shadow-lg), 0 0 0 2px rgba(20,120,242,.65) !important; opacity: .98; }
-.drop-placeholder { margin-left: 72px; min-height: 54px; border: 1.5px dashed #1680ff; border-radius: 12px; display: grid; place-items: center; color: #1478f2; background: rgba(20,120,242,.045); font-size: 11px; font-weight: 650; }
+.activity-card.is-resizing { box-shadow: 0 0 0 2px rgba(20,120,242,.68), var(--shadow-lg); z-index: 12; }
+.end-marker { position: absolute; z-index: 1; left: 84px; right: 0; transform: translateY(-50%); display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 10px; color: #858890; font-size: 10px; pointer-events: none; }
+.end-marker i { height: 1px; background: #d9dad6; }
+.end-marker strong { font-weight: 620; white-space: nowrap; }
+.is-drag-source .activity-card { opacity: .16; box-shadow: none; }
+.drag-floating { position: fixed !important; z-index: 1000; pointer-events: none; transform: rotate(.4deg) scale(1.012); box-shadow: var(--shadow-lg), 0 0 0 2px rgba(20,120,242,.52) !important; opacity: .97; cursor: grabbing; }
+.drop-placeholder { position: absolute; z-index: 20; left: 84px; right: 0; height: 3px; border-radius: 999px; background: #1680ff; box-shadow: 0 0 0 4px rgba(22,128,255,.10); color: #1478f2; }
+.drop-placeholder::before { content: ''; position: absolute; left: -4px; top: 50%; width: 9px; height: 9px; transform: translateY(-50%); border-radius: 50%; background: #1680ff; }
+.drop-placeholder span { position: absolute; right: 0; top: -20px; padding: 2px 6px; border-radius: 6px; background: #edf6ff; font-size: 9px; font-weight: 700; }
 body.is-reordering { user-select: none; cursor: grabbing; }
 .resize-tooltip { position: fixed; z-index: 1001; min-width: 62px; padding: 7px 9px; border-radius: 8px; background: #202124; color: #fff; box-shadow: var(--shadow-lg); font-size: 11px; font-weight: 700; text-align: center; pointer-events: none; }
 
@@ -174,6 +216,21 @@
 .stepper button:last-child { border-radius: 0 10px 10px 0; }
 .stepper input { border-left: 0; border-right: 0; border-radius: 0; padding: 4px; }
 .stepper input::-webkit-inner-spin-button { appearance: none; }
+.people-editor { border: 1px solid var(--line-strong); border-radius: 11px; background: #fff; padding: 9px; }
+.people-chip-list { display: flex; align-items: center; flex-wrap: wrap; gap: 7px; }
+.person-chip { min-height: 32px; padding: 4px 6px 4px 4px; border-radius: 999px; display: inline-flex; align-items: center; gap: 6px; background: #f5f5f3; color: #3b3d43; font-size: 11px; font-weight: 620; }
+.person-chip .person-avatar { width: 24px; height: 24px; font-size: 8px; }
+.person-remove { width: 25px; height: 25px; padding: 0; border: 0; border-radius: 50%; display: grid; place-items: center; background: transparent; color: #8c8f96; }
+.person-remove:hover { background: #e8e8e5; color: #4c4f56; }
+.person-remove .icon { width: 11px; height: 11px; }
+.people-add-trigger { min-height: 32px; padding: 0 9px; border: 0; border-radius: 999px; display: inline-flex; align-items: center; gap: 5px; background: transparent; color: #316fc2; font-size: 11px; font-weight: 680; }
+.people-add-trigger:hover { background: #eef5ff; }
+.people-add-trigger .icon { width: 13px; height: 13px; }
+.people-add-row { margin-top: 9px; display: grid; grid-template-columns: 1fr auto; gap: 7px; }
+.people-add-row[hidden] { display: none; }
+.people-add-row .people-add-input { min-height: 38px; padding: 8px 10px; }
+.people-add-row .button { min-height: 38px; }
+
 .lock-setting { border: 1px solid var(--line); border-radius: 12px; padding: 13px; display: flex; align-items: center; justify-content: space-between; gap: 12px; }
 .lock-setting > div { min-width: 0; display: flex; align-items: flex-start; gap: 9px; }
 .lock-setting > div > .icon { margin-top: 2px; color: var(--text-soft); }
@@ -216,25 +273,52 @@
 .toast--success { background: rgba(48,104,67,.96); }
 
 @media (max-width: 720px) {
-  .activity-card { grid-template-columns: 25px 3px minmax(0,1fr) auto; grid-template-areas: "drag accent main time" "drag accent meta meta"; gap: 8px; padding: 10px 10px 10px 5px; min-height: max(74px, var(--card-height)); }
-  .drag-handle { grid-area: drag; }
-  .accent-rule { grid-area: accent; height: 66%; }
-  .card-main { grid-area: main; gap: 5px; }
-  .card-time { grid-area: time; align-self: start; text-align: right; gap: 2px; }
+  .planner { padding-left: 10px; padding-right: 10px; }
+  .timeline-labels { grid-template-columns: 58px 1fr; gap: 10px; padding-left: 1px; }
+  .timeline-ruler::before { left: 58px; }
+  .timeline-tick span { right: calc(100% - 50px); }
+  .timeline-tick--hour span { font-size: 11.5px; }
+  .timeline-tick--half span { font-size: 10px; }
+  .timeline-tick--quarter span { font-size: 9px; }
+  .timeline-tick i { left: 53px; }
+  .timeline-tick--hour i { left: 51px; }
+  .timeline-plan { left: 68px; }
+  .end-marker, .drop-placeholder { left: 68px; }
+  .activity-card { display: block; padding: 9px 8px 7px 43px; }
+  .drag-handle { position: absolute; left: 3px; top: 8px; bottom: auto; width: 26px; height: 38px; }
+  .accent-rule { position: absolute; left: 35px; top: 9px; width: 3px; height: min(58px, calc(100% - 18px)); margin: 0; }
+  .card-main { position: absolute; left: 45px; right: 80px; top: 9px; margin: 0; gap: 3px; }
+  .card-heading { display: block; }
+  .card-heading h2 { padding: 0; font-size: 12.5px; line-height: 1.2; }
+  .card-actions { position: absolute; right: 4px; top: 4px; gap: 0; }
+  .card-time { position: absolute; right: 10px; top: 49px; margin: 0; text-align: right; gap: 1px; }
   .card-time-range { display: none; }
-  .card-time strong { font-size: 10px; }
-  .card-stage { grid-area: meta; justify-self: end; z-index: 1; }
-  .card-main .card-meta { position: absolute; left: 48px; right: 132px; bottom: 12px; display: block; }
+  .card-time strong { font-size: 9.5px; color: #64676f; }
+  .card-stage { position: absolute; right: 50px; top: 45px; margin: 0; z-index: 4; }
+  .stage-pill { min-height: 24px; padding: 3px 7px; gap: 4px; font-size: 9px; }
+  .stage-pill > .icon:first-chi