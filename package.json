{
  "name": "wedding-day-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/schedule.js && node --check src/api.js && node --check lib/server/auth.js && node --check lib/server/storage.js"
  },
  "engines": {
    "node": ">=20"
  }
}
