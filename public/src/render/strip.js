/**
 * The live strip is part of the day-of view and has no content yet. The region
 * exists now so that turning day-of on is a matter of filling it, not of moving
 * everything else down.
 */
export function renderStrip() {
  return '';
}
